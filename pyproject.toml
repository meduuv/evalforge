[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "evalforge"
version = "0.1.0"
description = "Evaluation case scoring utilities"
requires-python = ">=3.10"

[tool.hatch.build.targets.wheel]
packages = ["src/evalforge"]
